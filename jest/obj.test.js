describe('random', () => {

    test('should ', () => {
        const obj = { name: "akash" };
        const obj2 = { name: "akash" };
        expect(obj).toEqual(obj2);
    });
    test('should ', () => {
        const obj = { name: "akash" };
        const obj2 = { name: "akash" };
        expect(obj).toEqual(obj2);
    });
    test('should ', () => {
         const val = 10.98;
         expect(val).toBe(10.98);
        expect(obj).toEqual(obj2);
    });

})